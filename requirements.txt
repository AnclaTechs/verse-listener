# VerseListener - Python dependencies
# Install with: pip install -r requirements.txt
#
# This file is intentionally kept cross-platform.
# Do not replace it with `pip freeze` output from Linux, or Windows installs
# will pick up Linux-only packages such as `nvidia-cufile` and `python3-xlib`.

# Core UI
PyQt6>=6.11.0
python-dotenv>=1.0.1

# Audio
# Windows path: sounddevice (WASAPI)
# Linux path: sounddevice (PulseAudio/ALSA) or JACK if installed separately
sounddevice>=0.4.6
numpy>=1.24.0

# Speech-to-text
faster-whisper>=1.0.0
websocket-client>=1.8.0

# EasyWorship automation
pyautogui>=0.9.54
pygetwindow>=0.0.9

# Optional extras
# vosk>=0.3.45
# sentence-transformers>=3.0.0

# Optional Linux window-focus backends
# PyWinCtl
# python-xlib>=0.33
